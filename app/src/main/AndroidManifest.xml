<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"

        android:supportsRtl="true"
       >
        <activity android:name=".Phrases"
            android:label="Phrases"
            android:theme="@style/PhraseStyle"></activity>
        <activity android:name=".Colors"
            android:label="Colors"
            android:theme="@style/ColorStyle"/>
        <activity android:name=".FamilyMembers"
            android:label="Family Members"
            android:theme="@style/FamilyStyle"/>
        <activity
            android:name=".NumbersActivity"
            android:label="Numbers"
            android:theme="@style/NumberStyle"/>
        <activity android:name=".MainActivity"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>